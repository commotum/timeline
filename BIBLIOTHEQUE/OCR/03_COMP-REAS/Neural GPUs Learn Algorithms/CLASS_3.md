# NEURAL GPUS LEARN ALGORITHMS (Not specified in the paper.)
Source: Neural GPUs Learn Algorithms.md

## Core reasons
- Proposes a new neural architecture (Neural GPU) to learn algorithms, addressing limitations of sequential models.
- The contribution centers on a parallel convolutional recurrent computation mechanism for algorithmic tasks and generalization.

## Evidence extracts
- "We present a neural network architecture to address this problem: the *Neural GPU*. It is based on a type of convolutional gated recurrent unit and, like the NTM, is computationally universal. Unlike the NTM, the Neural GPU is highly parallel which makes it easier to train and efficient to run." (Abstract)
- "In this work we present a neural network model, the *Neural GPU*, that addresses the above issues. It is a Turing-complete model capable of learning arbitrary algorithms in principle, like a Neural Turing Machine. But, in contrast to Neural Turing Machines, it is designed to be as parallel and as shallow as possible." (1 Introduction)

## Classification
Class name: Computation & Reasoning Mechanism Proposal
Class code: 3

$$
\boxed{3}
$$
