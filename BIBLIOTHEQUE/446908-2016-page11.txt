                           Input Data Graph Learning takes                        creating reusable data transformations such as map, joins,
                               less than 1s (k <= 1000)                           aggregation, sorting etc. using examples. It also automati-
                  1000                                                            cally suggests some transformations based on the context
                in s)100                                                          of user interactions.  The Topes system [24] allows end-
                Time ( 10                                                         users to interactively implement abstractions (called topes)
                g   1                                                             for validating and transforming data in many diﬀerent for-
                ninr0.1                                                           mats.   It can also recommend some basic topes given a
                eaL0.01                                                           set of strings from a predeﬁned set of topes based on key-
                     1        10       100      1000     10000    100000          word matches [25]. The analysis of user context in Wran-
                               Sample size k of input rows                        gler and Topes’ recommendation, however, is not as rich as
                                                                                  that of BlinkFill in ﬁnding complex logical patterns that
              Figure 19: The time taken by the GenInpDataGraph                    are shared amongst a set of input strings. LAPIS [18] is a
              algorithm to compute the InputDataGraph for k ran-                  text editor that allows users to perform multiple selections
              domly sampled input strings in a large spreadsheet.                 using a set of positive and negative examples, and then per-
                                                                                  form simultaneous editing on the selections. It is also able
                                                                                  to identify some outlier selections corresponding to possible
              9.    RELATEDWORK                                                   incorrect generalization using machine learning [17]. Our
                 Comparison with FlashFill The domain-speciﬁc lan-                technique can complement LAPIS in not learning such in-
              guage and the Dag data structure for BlinkFill at top-              correct generalizations since such generalizations won’t be
              level are similar to that of FlashFill, but the key diﬀer-          part of large common pattern sequences in InputDataGraph.
              ence is in the way position expressions in the substring ex-        DataXFormer [1, 20] is an interactive data transformation
              pressions are represented and learnt using semi-supervised          and integration system that leverages Web tables and Web
              learning. The core technical challenge solved by BlinkFill          forms to perform syntactic and semantic data transforma-
              is in eﬃciently learning the set of all token sequences (and        tions. The class of transformations supported by DataX-
              sub-sequences) that are consistent with a set of strings in         Former are based on relational mapping of a set of strings
              a column using the InputDataGraph, and then using it to             (with some fuzzy matching capabilities), while the transfor-
              eﬃciently learn substring expressions. This results in a sig-       mations supported by BlinkFill involve concatenation of
              niﬁcant decrease in the ambiguity of possible transforma-           logical regular expression based substrings.
              tions that are consistent with a set of input-output examples         RecordAlignmentTheproblemofextractingrelational
              and makes the learning process dramatically more eﬃcient.           tables from lists using record alignment is also related to our
              This is crucial for increasing the applicability of FlashFill       technique of learning the set of common patterns shared by
              for longer strings (which is currently automatically disabled       a set of strings in a column. TEGRA [3] models the table
              by the Excel team for longer strings). Moreover, learning           extraction problem as an optimization problem to capture
              from data allows BlinkFill to learn token sequences con-            the conceptual goodness of an extracted table based on both
              sisting of arbitrary constant string tokens as opposed to a         syntactic and semantic information (using Web tables). Lis-
              ﬁnite pre-deﬁned tokens supported by FlashFill, which               tExtract [5] is another system that greedily segments each
              has been another major shortcoming of FlashFill. Unlike             string using various signals such as delimiters and seman-
              FlashFill, BlinkFill currently does not support condi-              tic information, and then uses a majority vote to guess the
              tionals and loops. For learning conditionals, a strategy sim-       correct number of segmented columns. BlinkFill, on the
              ilar to FlashFill can be implemented where the synthesis            other hand, learns the set of all regular expression based
              algorithm in BlinkFill clusters the input strings into dif-         patterns (and sub-patterns) shared by a set of strings, and
              ferent InputDataGraphs if there is no edge in the original          uses it to guide the synthesis algorithm for learning trans-
              InputDataGraph that corresponds to the substring extrac-            formations from examples. However, the techniques from
              tion task required by the input-output examples.                    TEGRAandListExtract can be used to enhance the semi-
                 Version-space Algebra based PBE Systems There                    supervised learning in BlinkFill by adding the semantic
              has been a lot of recent work on building PBE systems us-           knowledge and by using approximate inference algorithms
              ing the VSA methodology. The key idea in building these             for increasing robustness to noise.
              systems is to ﬁrst design an expressive DSL that allows for           Ranking in Program Synthesis The problem of am-
              decomposing the top-level speciﬁcation to make the learning         biguity arises in any inductive synthesis problem where the
              eﬃcient [8]. In addition to FlashFill, it has been used to          speciﬁcation is incomplete. PROSPECTOR [15] synthesizes jun-
              build PBE systems in the domains of semantic string trans-          gloid code fragments consisting of a chain of objects and
              formations [26], data extraction from unstructured text [14],       method calls from a given input type to an output type,
              and re-structuring of semi-structured spreadsheets [2]. Re-         and uses the length of code, generality of the output type,
              cently, a generic meta-algorithm FlashMeta [21] has been            and number of cross-package boundaries as the criterion for
              proposed to automatically generate an eﬃcient synthesis al-         ranking possible fragments. Lase [16] helps developers per-
              gorithm based on VSA from simply the description of the             form systematic program changes automatically by learning
              DSL, where the DSL is designed using a predeﬁned set of             from few examples. Recently, machine learning based tech-
              operators. All of these systems synthesize programs by con-         niques were used for learning the ranking function automat-
              sidering only the input-output examples and unlike our tech-        ically for FlashFill [27]. These ranking approaches use
              nique do not use the information present in other inputs in         manually deﬁned scoring (cost) functions, the speciﬁcation
              the learning algorithms.                                            constraint, and some global usage information for design-
                 StringTransformationusingExamplesandDemon-                       ing the ranking functions, but do not typically exploit the
              strations Data Wrangler [11] is an interactive system for           information present in other inputs for a given task.
                                                                             826
