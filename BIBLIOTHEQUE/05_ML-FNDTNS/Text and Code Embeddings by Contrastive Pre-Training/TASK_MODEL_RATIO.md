1. **Number of distinct tasks evaluated:** 23

   - "Figure 1. Average performance of unsupervised cpt-text models of different sizes across 22 tasks consisting of linear-probe classification, text search, and sentence similarity tasks." (Figure 1, Section 1. Introduction)
   - "We evaluate our code embedding models on the code search task using the CodeSearchNet benchmark (Husain et al., 2020)." (Section 3.3. Code Search)

2. **Number of trained model instances required to cover all tasks:** 9

   - "we use the *same* unsupervised model across all these tasks." (Section 3. Results)
   - "On linear-probe classification accuracy averaging over 7 tasks" (Abstract)
   - "When evaluated on linear-probe classification, the embeddings are used as features to train a linear classifier to solve a variety of downstream tasks." (Section 3.1.1. LINEAR PROBE CLASSIFICATION)
   - "Next, we train code embedding models (cpt-code) using the same recipe." (Section 1. Introduction)

$$
\boxed{
\frac{23\ \text{tasks}}{9\ \text{models}} = 2.56
}
$$
