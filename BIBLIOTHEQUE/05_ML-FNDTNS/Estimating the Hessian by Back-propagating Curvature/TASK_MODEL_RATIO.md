1. **Number of distinct tasks evaluated:** 2

> "In this section we test the accuracy of CP on a small neural network as we vary the number of samples." (Section 8.1 Accuracy Evaluation)

> "To test the effectiveness of CP in a more practical scenario, we focus on estimating the parameters of a Markov random field using the score matching technique." (Section 8.2 Score-Matching Experiments)

2. **Number of trained model instances required to cover all tasks:** 2

> "we tested both a network with random weights set by Gaussian noise with a variance of 0.01, and one trained to classify handwritten digits from the USPS dataset" (Section 8.1 Accuracy Evaluation)

> "we attempted to train a cRBM using stochastic gradient descent on minibatches of size 100." (Section 8.2 Score-Matching Experiments)

> "we trained two different versions of the model, one where we use the exact minibatch gradient, and one where we use approximate gradients via our estimator." (Section 8.2 Score-Matching Experiments)

3. **Task-Model Ratio = (1) / (2):**

$$
\boxed{
\frac{2\ \text{tasks}}{2\ \text{models}} = 1
}
$$
