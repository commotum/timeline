1. **Number of distinct tasks evaluated:** 1

"The goal of the agent is to maximize the discounted reward from the start state distribution D." (Section 2 Preliminaries)

2. **Number of trained model instances required to cover all tasks:** 1

"In this paper, we present the conservative policy iteration algorithm which finds an \"approximately\" optimal policy, given access to a restart distribution (which draws the next state from a particular distribution) and an approximate greedy policy chooser." (Abstract)

"(3) If the policy advantage is small (less than  $\varepsilon$ ), STOP and return  $\pi$ ." (Section 4.2 Answering question 3)

3. **Task–Model Ratio = (1) / (2)**

$$
\boxed{
\frac{1\ \text{tasks}}{1\ \text{models}} = 1
}
$$
