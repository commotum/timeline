1. **Number of distinct tasks evaluated:** 11

   "In this section, we present BERT fine-tuning results on 11 NLP tasks." (Section 4, "Experiments")

2. **Number of trained model instances required to cover all tasks:** 11

   "Each downstream task has separate fine-tuned models, even though they are initialized with the same pre-trained parameters." (Section 3, "BERT")

   "BERT and OpenAI GPT are single-model, single task." (Section 4.1, Table 1 caption)

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{11\ \text{tasks}}{11\ \text{models}} = 1
}
$$
