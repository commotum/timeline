# You Only Look Once: Unified, Real-Time Object Detection (Not specified in the paper.)
Source: You Only Look Once- Unified, Real-Time Object Detection.md

## Core reasons
- Proposes a unified single-network object detection approach rather than a dataset, positional encoding, or computation mechanism, indicating a model/architecture contribution.
- Focuses on end-to-end detection architecture and training methodology for real-time object detection, fitting a general ML modeling contribution.

## Evidence extracts
- "We present YOLO, a new approach to object detection. Prior work on object detection repurposes classifiers to perform detection. Instead, we frame object detection as a regression problem to spatially separated bounding boxes and associated class probabilities. A single neural network predicts bounding boxes and class probabilities directly from full images in one evaluation." (Abstract)
- "We unify the separate components of object detection into a single neural network." (Section 2. Unified Detection)

## Classification
Class name: ML Foundations & Principles
Class code: 5

$$
\boxed{5}
$$
