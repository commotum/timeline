1. **Number of distinct tasks evaluated:** 1 task (ARC puzzle transformation/prediction).

   - "The goal is to infer the transformation from the list of input/output grids and then apply the same transformation to a new input grid." (Section 1.1, "The Abstraction and Reasoning Corpus (ARC)")
   - "I evaluated each Mini-ARC model and strategy against a representative subset of the public ARC Evaluation Dataset." (Section 4, "Results")

2. **Number of trained model instances required to cover all tasks:** 1 model.

   - "I trained two models for evaluation: Mini-ARC-12 and Mini-ARC-v12." (Section 3.1, "Model Architecture")
   - "The only difference between the two is that Mini-ARC-v12 uses a modified embedding scheme..." (Section 3.1, "Model Architecture")

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{1\ \text{task}}{1\ \text{model}} = 1
}
$$
