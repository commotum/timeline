1. **Number of distinct tasks evaluated:** 24  
   Quote: “Overall, we achieved state-of-the-art performance on 18 out of the **24 tasks we consider**.” (Section 3.7, *Putting It All Together*)

2. **Number of trained model instances required to cover all tasks:** 24  
   Quote: “So far, we have been pre-training our model on a single unsupervised learning task before fine-tuning it individually on each downstream task.” (Section 3.5.2, *Multi-task Learning*)  
   Quote: “...we relax this goal somewhat and instead investigate methods for training on multiple tasks at once in order to eventually produce **separate parameter settings that perform well on each individual task**.” (Section 3.5.2, *Multi-task Learning*)

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{24\ \text{tasks}}{24\ \text{models}} = 1
}
$$
