1. **Number of distinct tasks evaluated:** 1  
“Given a data set consisting of pairs of integers  $\{(a_i,b_i)\}_{1\leq i\leq m}$ , where  $b_i\equiv a_is+e_i\pmod{p}$  and the 'noise' or 'error' values  $e_i$  are sampled from a centered discrete Gaussian distribution with standard deviation  $\sigma$ , the task is to find the unknown secret s.” (Section 2.1, “The task”)  
“We consider the noiseless version of the task described in Section 2.1 – namely, given a data set consisting of m pairs of integers  $\{(a_i, b_i)\}_{1 \leq i \leq m}$ , where  $b_i \equiv a_i s$  (mod p), the task is to find the unknown secret s.” (Section 3.1, “The task”)

2. **Number of trained model instances required to cover all tasks:** 1  
“We experiment with two different machine learning approaches to modular multiplication.” (Section 1, Introduction)  
“In Section 3 we explore an alternative approach using a sequence-to-sequence transformer...” (Section 1, Introduction)

3. **Task–Model Ratio = (1) / (2)**

$$
\boxed{
\frac{1\ \text{task}}{1\ \text{model}} = 1
}
$$
