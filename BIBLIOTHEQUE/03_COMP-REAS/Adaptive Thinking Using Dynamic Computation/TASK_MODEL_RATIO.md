1. **Number of distinct tasks evaluated:** 3

- "We demonstrate the effectiveness of this method on language modeling and computer vision tasks." (Section: **ABSTRACT**)
- "When applied to a transformer architecture, the approach achieves 95.8%/88.7% F1 scores on the SQuAD v1.1/v2.0 datasets" (Section: **ABSTRACT**)
- "The MIND-Transformer's results demonstrate its ability to outperform leading transformer models in both perplexity and downstream question-answering tasks" (Section: **4.4 EXPERIMENTS ON LANGUAGE MODELING TASKS**)

2. **Number of trained model instances required to cover all tasks:** 3

- "For computer vision applications, we implement the prediction network using a lightweight convolutional neural network (CNN) architecture." (Section: **3.3 Prediction Network Architecture**)
- "In the context of natural language processing, the prediction network is instantiated as a dual-layer Long Short-Term Memory (LSTM) network" (Section: **3.3 Prediction Network Architecture**)
- "MIND-Transformer For natural language processing tasks, we extend the prediction network to a Transformer architecture" (Section: **3.3 Prediction Network Architecture**)
- Whether one single jointly trained model instance is used for all tasks: **Not specified in the paper.**

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{3\ \text{tasks}}{3\ \text{models}} = 1
}
$$
