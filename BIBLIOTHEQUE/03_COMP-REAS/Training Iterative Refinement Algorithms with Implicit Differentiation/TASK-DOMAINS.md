# Object Representations as Fixed Points: Training Iterative Refinement Algorithms with Implicit Differentiation (Year not specified in the paper.)
Source: Training Iterative Refinement Algorithms with Implicit Differentiation.md

## Task Table
| Task | Input | In Dimension | In Dynamics | Attention Dynamic | State Dynamic | Output | Out Dimension | Out Dynamics |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Latent-set object representation inference | Images as pixel measurements (`x^n`, `x^{n,m}`) | 2D (x, y) (inferred) | Capped (inferred) | Dynamic (inferred) | Constructed (inferred) | Slot/object representations and assignments (`θ^n`, `φ^{n,m}`) | 1D (t) (inferred) | Fixed (inferred) |
| Image reconstruction through the slot bottleneck | Input images compressed to a grid of discrete tokens | 2D (x, y) (inferred) | Capped (inferred) | Dynamic (inferred) | Constructed (inferred) | Reconstructed latent code-vectors/images | 2D (x, y) (inferred) | Capped (inferred) |
| Object property prediction | Images (inferred) | 2D (x, y) (inferred) | Not specified in the paper. | Dynamic (inferred) | Constructed (inferred) | Object properties | 1D (t) (inferred) | Not specified in the paper. |
| Segmentation mask prediction | Images (inferred) | 2D (x, y) (inferred) | Not specified in the paper. | Dynamic (inferred) | Constructed (inferred) | Segmentation masks / attention masks | 2D (x, y) (inferred) | Not specified in the paper. |

## Summary
The paper centers on object-centric learning from images, with a core task of inferring latent slot-based sets from pixel inputs. Its main evaluated downstream task is image reconstruction through a slot bottleneck on CLEVR-Mirror, ShapeStacks, and COCO, and it also reports object property prediction plus segmentation mask prediction with slot attention. The supported dimensional coverage is primarily 2D (x, y) on input images, while slot outputs are fixed-size indexed sets (inferred) and mask/reconstruction outputs remain image-like 2D fields. Attention and state are best classified as Dynamic and Constructed (inferred) because the model iteratively updates slot and assignment variables from random slot initialization.

## Evidence
### Task: Latent-set object representation inference
- "In this work, we consider the case of iterative refinement applied to representation learning of latent sets, which has primarily been applied to learning representations of objects from pixels [24]-[26], [35], [39], [57], [61], [62], [70]." (Section 1 Introduction)
- "Iterative refinement for inferring representations of latent sets. Current work on inferring representations of latent sets is motivated by learning to represent objects – which we consider independent and symmetric entities – from perceptual input. Because mixture models are also defined to have a priori independent and symmetric mixture components, they have been the model of choice for representing entities: thus these iterative methods model each datapoint  $x^n$  (e.g. image) as a set of independent sensor measurements  $x^{n,m}$  (e.g. pixels) which are posited as having been generated from a mixture model whose components represent the entities. Under a clustering lens, the problem reduces to finding the K groups of cluster parameters  $\theta^n := \{\theta^{n,k}\}_{k=1}^K$  and cluster assignments  $\phi^{n,m} := \{\phi^{n,m,k}\}_{k=1}^K$  that were responsible for the measurements  $x^{n,m}$  of the datapoint  $x^n$ ." (Section 3 Background)
- "A network f breaks symmetry among components by alternately updating  $\theta^n$  and  $\phi^{n,m}$  starting from independent randomly initialized  $\theta^{n,k}$ 's." (Section 3 Background)
- Inference: In Dimension is inferred as 2D (x, y) from "image"/"pixels" wording; Out Dimension and Out Dynamics are inferred from the indexed fixed-slot form "$\{\theta^{n,k}\}_{k=1}^K$"; Attention Dynamic and State Dynamic are inferred from iterative updates of $\theta$ and $\phi$ from random initialization; In Dynamics is inferred as Capped from bounded tested image resolutions ("96x96" and "64x64"; Section 5.1, Implementation details).

### Task: Image reconstruction through the slot bottleneck
- "SLATE uses a discrete VAE [50] to compress an input image into a grid of discrete tokens. These tokens index into a codebook of latent code-vectors, which, after applying a learned position encoding, serve as the input to the slot attention module. An Image GPT decoder [10] is trained with a cross-entropy loss to autoregressively reconstruct the latent code-vectors, using the outputted slots from slot attention as queries and the latent code-vectors as keys/values." (Section 5.1 Experimental setup)
- "Figure 4: **Qualitative results.** Across three datasets, optimizing SLATE with implicit differentiation leads to improved image reconstructions through the slot bottleneck." (Section 5.3)
- "Table 3: Quantitative metrics for image reconstruction through the slot bottleneck." (Section 5.3)
- Inference: 2D input/output dimensions are inferred from image and token-grid language; Capped dynamics are inferred from the paper’s bounded image resolutions ("largest image size ... 96x96" and results at "96x96 and 64x64" in Section 5.1); Attention Dynamic and State Dynamic are inferred from slot attention producing and refining slots used by the decoder.

### Task: Object property prediction
- "Fifth, when integrated with the original slot attention encoders and decoders from Locatello et al. [39], implicit differentiation substantially improves object property prediction and continues to predict intuitive segmentation masks as the vanilla slot attention." (Section 1 Introduction)
- "Futhermore, Fig. 7 shows that using only a single iteration is not enough to improve optimization of vanilla slot attention for the object property prediction task used by Locatello et al. 39." (Section 5.5)
- "Figure 7: We outperform vanilla slot attention on object property prediction (see Locatello et al. [39])." (Section 5.5)
- Inference: Input and In Dimension are inferred as image-based 2D because this evaluation is performed with slot-attention encoders/decoders; Out Dimension is inferred as 1D (t) for per-object property outputs over slot/object indices; Attention Dynamic and State Dynamic are inferred from the same iterative slot-attention mechanism. In/Out Dynamics for this specific task are not explicitly specified in the OCR.

### Task: Segmentation mask prediction
- "Fifth, when integrated with the original slot attention encoders and decoders from Locatello et al. [39], implicit differentiation substantially improves object property prediction and continues to predict intuitive segmentation masks as the vanilla slot attention." (Section 1 Introduction)
- "We sought to check whether implicit differentiation still preserves the quality of the segmentation masks produced by the original slot attention architecture by Locatello et al. [39], which uses a spatial broadcast decoder [63] rather than a transformer decoder as SLATE does. It indeed does (Fig. 8), suggesting that our findings are not specific to SLATE but apply to slot attention more broadly." (Section 5.6)
- "The attention masks for both vanilla and implicit SLATE furthermore do not appear to correspond consistently to coherent objects in COCO but rather patches on the image that do not immediately seem to match with our human intuition of what constitutes a visual entity." (Section 5.3, Qualitative analysis)
- Inference: Input and In Dimension are inferred as image-based 2D from the segmentation/attention-mask context; Out Dimension is inferred as 2D (x, y) because masks are spatial; Attention Dynamic and State Dynamic are inferred from slot attention’s iterative slot/assignment updates. In/Out Dynamics are not explicitly specified for this task.
