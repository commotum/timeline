1. **Number of distinct tasks evaluated:** 3
- "We conducted experiments in two domains. The first domain consists of randomly generated mazes." and "In these mazes, we considered two tasks." (Section 5, Experiments)
- "In the first task, the input was a  $13 \times 13$ maze and a random initial position and the goal is to predict a trajectory generated by a simple fixed deterministic policy." (Section 5, Experiments)
- "In the second random-maze task the goal was to predict for each of the cells on the diagonal of a  $20 \times 20$  maze (top-left to bottom-right) whether it is connected to the bottom-right corner." (Section 5, Experiments)
- "Our second domain is a simulation of the game of pool, using four balls and four pockets." and "The goal is to simultaneously learn to predict future events for each of the four balls, given 5 RGB frames as input." (Section 5, Experiments)

2. **Number of trained model instances required to cover all tasks:** 3
- "In the first experiment we trained a predictron to predict trajectories generated by a simple deterministic policy in  $13 \times 13$  random mazes with random starting positions." (Section 5.1)
- "In the next set of experiments, we tackle the problem of predicting connectivity of multiple pairs of locations in a random maze, and the problem of learning many different value functions from our simulator of the game of pool." (Section 5.2)
- "For the illustrative maze experiment in Section 5.1, a smaller network architecture is employed with 6 model steps and convolutional feature maps of 16 channels." (Appendix A, Architecture)

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{3\ \text{tasks}}{3\ \text{models}} = 1
}
$$
