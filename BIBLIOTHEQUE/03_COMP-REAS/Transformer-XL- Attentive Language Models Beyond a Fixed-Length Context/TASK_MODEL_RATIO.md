1. **Number of distinct tasks evaluated: 1**

   - "Given a corpus of tokens  $\mathbf{x} = (x_1, \dots, x_T)$ , the task of language modeling is to estimate the joint probability  $P(\mathbf{x})$" (Section 3 Model).
   - "Transformer-XL obtained strong results on five datasets, varying from word-level to character-level language modeling." (Section 1 Introduction).

2. **Number of trained model instances required to cover all tasks: 1 model**

   - "Given a corpus of tokens  $\mathbf{x} = (x_1, \dots, x_T)$ , the task of language modeling is to estimate the joint probability  $P(\mathbf{x})$" (Section 3 Model).
   - Whether a single jointly trained instance was used across all five datasets is **"Not specified in the paper."**

3. **Task–Model Ratio = (1) / (2)**

$$
\boxed{
\frac{1\ \text{task}}{1\ \text{model}} = 1
}
$$
