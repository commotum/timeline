1. **Number of distinct tasks evaluated:** 8

> "Results on patterns generated by \"counting\" algorithms are shown in Table 2."
> *(Section 5.1 Learning simple algorithmic patterns)*
>
> "| method                     | $a^nb^n$ | $a^nb^nc^n$ | $a^n b^n c^n d^n$ | $a^n b^{2n}$ | $a^n b^m c^{n+m}$ |"
> *(Table 2, Section 4.2 Pushdown network)*
>
> "**Memorization.** Figure 2 shows results on memorization for a dictionary with two elements."
> *(Section 5.1 Learning simple algorithmic patterns)*
>
> "**Binary addition.** Given a sequence representing a binary addition, e.g., \"101+1=\", the goal is to predict the result, e.g., \"110.\" where \".\" represents the end of the sequence."
> *(Section 5.1 Learning simple algorithmic patterns)*
>
> "We compare Stack RNN with RNN, LSTM and SRCN [25] on the standard language modeling dataset Penn Treebank Corpus."
> *(Section 5.2 Language modeling)*

2. **Number of trained model instances required to cover all tasks:** 8

> "Given an algorithm with short description length, we generate sequences and concatenate them into longer sequences."
> *(Section 5.1 Learning simple algorithmic patterns)*
>
> "Stack RNN has 100 units and 10 stacks, and List RNN has 10 lists."
> *(Section 5.1 Learning simple algorithmic patterns, Memorization)*
>
> "Stack RNN has 100 hidden units with 10 stacks."
> *(Section 5.1 Learning simple algorithmic patterns, Binary addition)*
>
> "Table 4: Comparison of RNN, LSTM, SRCN [25] and Stack RNN on Penn Treebank Corpus. We use the recurrent matrix R in Stack RNN as well as 100 hidden units and 60 stacks."
> *(Section 5.2 Language modeling)*
>
> "Not specified in the paper."

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{8\ \text{tasks}}{8\ \text{models}} = 1
}
$$
