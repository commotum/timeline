1. **Number of distinct tasks evaluated:** 2

   "My approach scored 77.1% on ARC-AGI-1 and 26.0% on ARC-AGI-2, both beating results of frontier models and previous bespoke systems [Note]." (Opening text before "1. Background")

   "ARC-AGI-2, which was launched on 25 March 2025, is the next iteration which tests more advanced AI capabilities such as symbolic interpretation (can a machine interpret symbols as having meaning beyond their visual patterns), compositional

reasoning (can a machine combine multiple learned rules that interact with each other), and contextual rule application (can a machine apply rules differently based on context)." (Background, **ARC-AGI**)

2. **Number of trained model instances required to cover all tasks:** 1

   "I designed a <u>DreamCoder</u>-inspired, LLM-assisted program synthesis system that can solve increasingly harder tasks by leveraging learned concepts in an expanding library of programs." (Opening text before "1. Background")

   "Starting from an empty library, my system loops through each task to prompt an LLM for Python program(s) that can solve all of the training examples." (My Approach, **Architecture**)

3. **Task–Model Ratio = (1) / (2):** 2

$$
\boxed{
\frac{2\ \text{tasks}}{1\ \text{model}} = 2
}
$$
