1. **Number of distinct tasks evaluated:** 22

   Verbatim quote: "We evaluate our method on all 22 tasks for language modeling from the Pile benchmark." (Section 1.1, "OUR CONTRIBUTIONS")

2. **Number of trained model instances required to cover all tasks:** 1

   Verbatim quote: "Figure 5: Bits per byte results on all Pile tasks for a small GPT-2 model (117M parameters) before and after test-time training on 50 nearest neighbors." (Section 5, Figure 5)

   Verbatim quote: "After fine-tuning the model on the current test instance, we reset its trainable parameters to their original states - those of the pre-trained base model." (Section 4, "TEST-TIME TRAINING ON NEAREST NEIGHBORS")

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{22\ \text{tasks}}{1\ \text{model}} = 22
}
$$
