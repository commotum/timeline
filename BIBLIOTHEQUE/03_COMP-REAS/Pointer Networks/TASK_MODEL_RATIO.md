1. **Number of distinct tasks evaluated: 3.**
   Verbatim: "We show Ptr-Nets can be used to learn approximate solutions to three challenging geometric problems - finding planar convex hulls, computing Delaunay triangulations, and the planar Travelling Salesman Problem" (Abstract).
   Verbatim: "we name Pointer Networks (Ptr-Nets), can be trained to output satisfactory solutions to three combinatorial optimization problems – computing planar convex hulls, Delaunay triangulations and the symmetric planar Travelling Salesman Problem (TSP)" (Section 1 Introduction).

2. **Number of trained model instances required to cover all tasks: 3 models.**
   Verbatim: "we used virtually the same architecture throughout all the experiments and datasets" (Section 4.1 Architecture and Hyperparameters).
   Verbatim: "having the same model hyperparameters operate on all the problems" (Section 4.1 Architecture and Hyperparameters).
   A single jointly trained model covering all tasks: Not specified in the paper.

3. **Task–Model Ratio**

$$
\boxed{
\frac{3\ \text{tasks}}{3\ \text{models}} = 1
}
$$
