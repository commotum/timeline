1. **Number of distinct tasks evaluated:** 2  
   Verbatim evidence: "We experimented with the Neural Transducer on the toy task of adding two three-digit decimal numbers." (Section 4.1, Addition Toy Task)  
   Verbatim evidence: "We used TIMIT, a standard benchmark for speech recognition, for our larger experiments." (Section 4.2, TIMIT)  
   Verbatim evidence: "We applied the model to a toy task of addition, and to a phone recognition task..." (Section 6, Conclusion)

2. **Number of trained model instances required to cover all tasks:** 2  
   Verbatim evidence: "The model is able to learn this task with a very small number of units (both encoder and transducer are 1 layer unidirectional LSTM RNNs with 100 units)." (Section 4.1, Addition Toy Task)  
   Verbatim evidence: "We trained a Neural Transducer with three layer LSTM RNN coupled to a three LSTM layer unidirectional encoder RNN, and achieved a PER of 20.8% on the TIMIT test set." (Section 4.2, TIMIT)

3. **Task–Model Ratio = (1) / (2):**

$$
\boxed{
\frac{2\ \text{tasks}}{2\ \text{models}} = 1
}
$$
