# Lie-Access Neural Turing Machines (LANTM) (Year not specified)
Source: Lie-Access Neural Turing Machines (LANTM).md

## Binary decision
Decision: TRANSFORMER-NO
Confidence: high
Basis: abstract-aux-only

## Why
- The abstract describes a Lie-access neural Turing machine with an LSTM controller and external memory, not Transformer blocks or self-attention as the core architecture.
- Auxiliary analyses describe LANTM and task-wise LSTM-based setups, with no indication that Transformer-style self-attention is central to the model used for main results.
- The extending-dimensions analysis file was unavailable (`MISSING`), but the abstract plus available auxiliary files were sufficient for a high-confidence decision.

## Evidence
- "To experiment with this approach, we implement a neural Turing machine with an LSTM controller and several versions of Lie-access memory, which we call Lie-access neural Turing machines (LANTM)." (Abstract, `Lie-Access Neural Turing Machines (LANTM).md`)
- "In this paradigm, memory is accessed using a continuous head in a key-space manifold. The head is moved via Lie group actions, such as shifts or rotations, generated by a controller..." (Abstract, `Lie-Access Neural Turing Machines (LANTM).md`)
- "For all tasks, the LSTM baseline has 1 to 4 layers, each with 256 cells." (Section 5 Model Setup quoted in `TASK_MODEL_RATIO.md`)
- "The paper evaluates LANTM on eight algorithmic sequence-transduction tasks..." (`TASK-DOMAINS.md`, Summary)

## Pass accounting
Pass 1 (abstract + auxiliary files): performed - sufficient evidence for a high-confidence TRANSFORMER-NO decision.
Pass 2 (targeted source scan): skipped - not needed because Pass 1 was sufficient; extending-dimensions analysis was unavailable (`MISSING`).
