1. **Number of distinct tasks evaluated:** 1

> “Learning a natural language interface for database tables is a challenging task that involves deep language understanding and multi-step reasoning.” (ABSTRACT)

> “We apply Neural Programmer on the WikiTableQuestions dataset (Pasupat & Liang, 2015) and compare it to different non-neural baselines including a natural language semantic parser developed by Pasupat & Liang (2015).” (Section 3: EXPERIMENTS)

> “To understand the difficulty of the task for neural network models, we experiment with two neural network baselines: the sequence-to-sequence model (Sutskever et al., 2014) and a modified version of the pointer networks (Vinyals et al., 2015).” (Section 3.3.1: NEURAL NETWORK BASELINES)

2. **Number of trained model instances required to cover all tasks:** 1

> “The main experimental result in this paper is that a single Neural Programmer model achieves 34.2% accuracy using only 10,000 examples with weak supervision.” (ABSTRACT)

> “Neural programmer makes use of three variables: row selector, scalar answer and lookup answer which are updated at every timestep.” (Section 2.2: OUTPUT AND ROW SELECTOR)

> “An ensemble of 15 models, with a trivial combination technique, achieves 37.7% accuracy, which is competitive to the current state-of-the-art accuracy of 37.1% obtained by a traditional natural language semantic parser.” (ABSTRACT)

3. **Task–Model Ratio = (1) / (2)**

$$
\boxed{
\frac{1\ \text{tasks}}{1\ \text{models}} = 1
}
$$
