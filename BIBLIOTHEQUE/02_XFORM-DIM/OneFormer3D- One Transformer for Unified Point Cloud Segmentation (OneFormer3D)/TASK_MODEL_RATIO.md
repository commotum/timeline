"In this paper, we proposed a novel transformer-based framework, OneFormer3D, that unifies three 3D point cloud segmentations tasks: instance, semantic, and panoptic." (Section 5. Conclusion)

"Finally, we come up with a single unified model trained only once, that outperforms 3D semantic, 3D instance, and 3D panoptic segmentation methods – even though they are specifically tuned for each task." (Section 1. Introduction)

$$
\boxed{
\frac{3\ \text{tasks}}{1\ \text{model}} = 3
}
$$
