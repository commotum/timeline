Quote (Section 3 Method): "Besides rendering novel views, our model can also perform camera pose estimation, i.e., the \"inverse\" of the view synthesis task: given a set of context views and a query image, the model outputs the camera pose from which the image was taken."
Quote (Section 1 Introduction): "Thanks to the branching attention, our model can both render a novel view from a given pose and predict the pose for a given image."

Number of distinct tasks evaluated: 2 (novel view synthesis, camera pose estimation)
Number of trained model instances required to cover all tasks: 1

$$
\boxed{
\frac{2\ \text{tasks}}{1\ \text{model}} = 2
}
$$
