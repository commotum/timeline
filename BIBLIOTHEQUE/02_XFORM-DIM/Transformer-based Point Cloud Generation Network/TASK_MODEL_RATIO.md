Quote (ABSTRACT): "In this paper, we propose a novel transformer-based 3D point cloud generation network to generate realistic point clouds."

Quote (Section 4.2.3 Classification results): "we first train our model with all the data of ShapeNet"; "train a linear SVM for classification on ModelNet10 and ModelNet40."

Number of distinct tasks evaluated: 2 (point cloud generation; classification).

Number of trained model instances required to cover all tasks: 2 (the trained model for generation; the trained linear SVM for classification).

$$
\boxed{
\frac{2\ \text{tasks}}{2\ \text{models}} = 1
}
$$
