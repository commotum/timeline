"We employ intersection over union (IoU) to evaluate the scene completion quality, regardless of the allocated semantic labels." (Evaluation metrics)
"We use the mean IoU (mIoU) of 19 semantic classes to assess the performance of semantic segmentation." (Evaluation metrics)
"In summary, the overall objective is to learn a neural network  $\Theta$  to generate a semantic voxel  $\mathbf{Y}_t = \Theta(\mathbf{I}_t)$  as close to the ground truth  $\hat{\mathbf{Y}}_t$  as possible." (Problem setup)

Number of distinct tasks evaluated: 2 (scene completion; semantic segmentation).
Number of trained model instances required to cover all tasks: 1.

$$
\boxed{
\frac{2\ \text{tasks}}{1\ \text{model}} = 2
}
$$
