"TTT is about training the model when we submit it, i.e. on the hidden test data. We can also evaluate its effectiveness using the public evaluation tasks." (Section 5 TTT)

"we noticed that if we trained on 100 evaluation tasks at a time, then we got better results than training on 400 tasks at a time." (Section 5 TTT)

"The model has an embedding layer that turns tasks ids into vectors that are added to all color embeddings." (Section 3 Task Specific Modeling)

"Retraining the full model with new task embeddings was the way to go." (Section 5 TTT)

Number of distinct tasks evaluated: Not specified in the paper.

Number of trained model instances required to cover all tasks: Not specified in the paper.

$$
\boxed{
\frac{\text{Not specified in the paper.}}{\text{Not specified in the paper.}} = \text{Not specified in the paper.}
}
$$
