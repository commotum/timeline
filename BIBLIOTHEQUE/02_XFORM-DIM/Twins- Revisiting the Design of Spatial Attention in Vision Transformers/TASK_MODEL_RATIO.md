"## 4.1 Classification on ImageNet-1K" (Section 4.1 Classification on ImageNet-1K)

"#### 4.2 Semantic Segmentation on ADE20K" (Section 4.2 Semantic Segmentation on ADE20K)

"#### 4.3 Object Detection and Segmentation on COCO" (Section 4.3 Object Detection and Segmentation on COCO)

"**Table 4** – Object detection and instance segmentation performance on the COCO val2017 dataset using the Mask R-CNN framework." (Table 4)

1. Number of distinct tasks evaluated: 4 (image-level classification; semantic segmentation; object detection; instance segmentation).
2. Number of trained model instances required to cover all tasks: 3 (one classification model, one semantic segmentation model, and one Mask R-CNN model that reports both object detection and instance segmentation).
3. Task-Model Ratio = 4 / 3 = 1.33.

$$
\boxed{
\frac{4\ \text{tasks}}{3\ \text{models}} = 1.33
}
$$
