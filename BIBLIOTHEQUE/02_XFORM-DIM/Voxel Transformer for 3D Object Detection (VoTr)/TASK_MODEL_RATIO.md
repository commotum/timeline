1. Number of distinct tasks evaluated: 2 — "we evaluate Voxel Transformer on the commonly used Waymo Open dataset [30] and the KITTI [8] dataset." (Section 4. Experiments)
2. Number of trained model instances required to cover all tasks: 2 — "On the KITTI dataset, VoTr-SSD and VoTr-TSD are trained with the batch size 32 and 16 respectively" (Section 4.1. Experimental Setup); "On the Waymo Open dataset, we uniformly sample 20% frames for training and use the full validation set for evaluation following [26]." (Section 4.1. Experimental Setup)
3. Task–Model Ratio:

$$
\boxed{
\frac{2\ \text{tasks}}{2\ \text{models}} = 1
}
$$
