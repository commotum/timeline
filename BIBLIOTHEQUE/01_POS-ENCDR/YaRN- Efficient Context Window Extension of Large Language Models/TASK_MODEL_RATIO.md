Number of distinct tasks evaluated: 7.

Section 4.3 Evaluation (verbatim):
> The evaluations focus on three aspects:
>
> - 1. the perplexity scores of fine-tuned models with extended context window,
> - 2. the passkey retrieval task on fine-tuned models,
> - 3. the common LLM benchmark results of fine-tuned models,

Section 4.3.1 Long Sequence Language Modeling (verbatim):
> To evaluate the long sequence language modeling performances, we use the GovReport [18] and Proof-pile [4] datasets both of which contain many long sequence samples.

Section 4.3.2 Passkey Retrieval (verbatim):
> The passkey retrieval task as defined in [25] measures a model's ability to retrieve a simple passkey (i.e., a five-digit number) from amongst a large amount of otherwise meaningless text.

Section 4.3.3 Standardized Benchmarks (verbatim):
> Specifically, we use 25-shot ARC-Challenge [11], 10-shot HellaSwag [41], 5-shot MMLU [17], and 0-shot TruthfulQA [23].

Number of trained model instances required to cover all tasks: 1.

Section 4.3 Evaluation (verbatim):
> The evaluations focus on three aspects:
>
> - 1. the perplexity scores of fine-tuned models with extended context window,
> - 2. the passkey retrieval task on fine-tuned models,
> - 3. the common LLM benchmark results of fine-tuned models,

$$
\boxed{
\frac{7\ \text{tasks}}{1\ \text{model}} = 7
}
$$
