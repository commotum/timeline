Quote (Section E.1 Gene Perturbation Prediction): "The Gene Perturbation Prediction (GPP) experiment leverages learned gene representations to predict the effects of targeted perturbations. These embeddings, generated by foundational models, serve as input to downstream classification heads that predict perturbation status, thereby elucidating gene function and regulatory network dependencies."

Quote (Section E.2 Cell Type Annotation): "Under the fine-tuning setting, we append an additional classifier to the cell embeddings generated by each model and perform supervised fine-tuning on the model parameters to optimize task-specific performance."

Quote (Section E.3 Cell Clustering): "We employed the k-means algorithm to obtain the cell clusters and subsequently evaluated the clustering performance using three commonly used metrics, including ARI [90], NMI [91] and ASW [92]."

Quote (Section E.4 Age Prediction): "Following established DNA methylation foundational models [77], we fine-tuned both our model and MethylGPT using a ResNet1D prediction head."

Number of distinct tasks evaluated: 4 (gene perturbation prediction; cell type annotation; cell clustering; age prediction).

Number of trained model instances required to cover all tasks: 4.

$$
\boxed{
\frac{4\ \text{tasks}}{4\ \text{models}} = 1
}
$$
