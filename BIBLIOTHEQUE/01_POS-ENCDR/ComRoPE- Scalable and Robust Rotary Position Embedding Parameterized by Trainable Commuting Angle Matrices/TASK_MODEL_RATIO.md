1. Number of distinct tasks evaluated: 3. "We first assess their scalability in 2D image classification across different resolutions." (Section 4. Experiments) "Additionally, we conduct object detection experiments to demonstrate the generalizability of our approach." (Section 4. Experiments) "we perform 3D classification experiments, which are detailed in Appendix B." (Section 4. Experiments)
2. Number of trained model instances required to cover all tasks: 3. "we utilize a standard Vision Transformer (ViT-B/16) architecture" (Section 4.1.1 Setup) "We adopt ViT-S as our backbone and apply Com-RoPE to the attention layers." (Section 4.2 Object detection) "we modified the model parameters to make it possible to conduct experiments" and "using a smaller model is more appropriate." (Section C.2 Configuration of 3D classification)
3. Task–Model Ratio = (1) / (2)

$$
\boxed{
\frac{3\ \text{tasks}}{3\ \text{models}} = 1
}
$$
